# Screenshots pending recapture

No screenshots are pending recapture. The cumulative window (parent release
`v2026.626.0` → quarantine cutoff `f019f54b`) touches UI source files, but none
that any registry entry `depends_on`, so nothing became newly stale. The registry
remains stamped against `master` `4c6c0c6ad048838dda4a67e1aca43aa37a6fcf0d`
(tagged `v2026.626.0`).

**Advisory:** the accent-bubble contrast fix (`#8689`, `4a2447da`) modifies global
`index.css` and issue chat-thread rendering. The registry intentionally does not
track `index.css` as a dependency (it would mark every screenshot stale on any CSS
tweak), so this change is not auto-flagged. If an issue-chat screenshot looks off
after the next release, recapture manually.

This file is regenerated by `/sync-docs` (nightly): if parent UI source files
that screenshots `depends_on` change, it will repopulate with the new stale set.
Recapture with `npm run screenshots:refresh` (stale only) — output PNGs land in a
PR for human review; never auto-pushed.
