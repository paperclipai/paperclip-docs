# SCREENSHOTS_PENDING — nightly docs sync

Regenerated by `/sync-docs` each run. Lists screenshots whose `depends_on` parent paths changed in
the current diff window (`v2026.722.0` → `4813ed3f`). Recapture with `npm run screenshots:refresh`
(output PNGs land in a PR for human review — never auto-pushed).

**152 of 310 screenshots are flagged stale.** That number is inflated because this window spans a
whole release cycle of UI churn, not just this run's 59-commit delta. Recapturing everything is a
`npm run screenshots:refresh:all` decision, not a per-entry chase. The entries below are split into
what's **relevant to the pages changed this run** vs. the **broad churn** behind the rest.

## Relevant to this run's doc changes (recapture these)

Tied to the P1 Import/Export work and the R1 Cloud Sync deprecation:

| Screenshot | Route | Changed dependency |
| --- | --- | --- |
| `light/company/export.png` · `dark/company/export.png` | `/…/company/export` | `ui/src/pages/CompanyExport.tsx` |
| `light/company/import.png` · `dark/company/import.png` | `/…/company/import` | `ui/src/pages/CompanyImport.tsx` |
| `light/export-import/export-dialog.png` · `dark/…` | `/…/company/export` | `ui/src/pages/CompanyExport.tsx` |
| `light/export-import/import-dialog.png` · `dark/…` | `/…/company/import` | `ui/src/pages/CompanyImport.tsx` |
| `light/experimental/cloud-upstream.png` · `dark/…` | `/…/company/settings/cloud-upstream` | `ui/src/pages/CloudUpstream.tsx` |

Note: the two `cloud-upstream.png` shots belong to the **deprecated** Cloud Sync page (R1). They will
be removed along with those pages at release time — no need to recapture them.

## Broad UI churn (the other ~142 stale entries)

These are stale because widely-used pages changed across the release cycle. Recapture in bulk when
convenient; none map to a doc edit made this run. Top drivers (stale-shot count → changed dependency):

- 24× `ui/src/pages/AgentDetail.tsx`
- 24× `ui/src/pages/IssueDetail.tsx`
- 22× `ui/src/pages/Inbox.tsx`
- 16× `ui/src/pages/Costs.tsx`
- 14× `ui/src/pages/Dashboard.tsx`
- 10× `ui/src/pages/ProjectDetail.tsx`
- 6× `ui/src/pages/RoutineDetail.tsx`
- 4× each: `CompanySettings.tsx`, `InstanceExperimentalSettings.tsx`, `Secrets.tsx`, `Timeline.tsx`
