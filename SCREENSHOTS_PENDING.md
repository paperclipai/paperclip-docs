# Screenshots pending recapture

No screenshots are pending recapture. The cumulative window (parent release
`v2026.626.0` → `master` HEAD `e6407b32`) touches no UI source files that any
registry entry `depends_on`, so nothing became newly stale. The registry remains
stamped against `master` `4c6c0c6ad048838dda4a67e1aca43aa37a6fcf0d`
(tagged `v2026.626.0`).

This file is regenerated by `/sync-docs` (nightly): if parent UI source files
that screenshots `depends_on` change again, it will repopulate with the new
stale set. Recapture with `npm run screenshots:refresh` (stale only) — output
PNGs land in a PR for human review; never auto-pushed.
