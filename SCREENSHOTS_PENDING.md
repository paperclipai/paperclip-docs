# Screenshots pending recapture

Generated by `/sync-docs` **nightly** run. Window: parent `v2026.720.0` (`903bd15`) → settled cutoff `230126d8`.

**4 of 310** registry entries are stale — a targeted refresh, not an overhaul:

```sh
npm run screenshots:refresh
```

The captured PNGs go to a PR for human review; they are never auto-pushed.

The previous release cycle's full overhaul is done: 258 entries now carry `captured_sha` `903bd157` (the `v2026.720.0` commit), so the "248 of 298 stale" state this file described before no longer applies.

---

## Stale entries

### `secrets` (4)

`ui/src/pages/Secrets.tsx` was substantially reworked by PR #9913 (folder view) — the screen now has a Folders/Flat toggle, a breadcrumb row, and folder rows. Both captures below are of that screen, so both are out of date.

- `light/secrets/user-secret-definition.png` — deps changed: `ui/src/pages/Secrets.tsx`
- `dark/secrets/user-secret-definition.png` — deps changed: `ui/src/pages/Secrets.tsx`
- `light/secrets/per-user-value-entry.png` — deps changed: `ui/src/pages/Secrets.tsx`, `ui/src/pages/secrets/MyUserSecretsTab.tsx`
- `dark/secrets/per-user-value-entry.png` — deps changed: `ui/src/pages/Secrets.tsx`, `ui/src/pages/secrets/MyUserSecretsTab.tsx`

`MyUserSecretsTab.tsx` now renders names through `SecretPathName`, which mutes the directory portion and bolds the leaf — so slash-named secrets look different on the My secrets tab even where nothing else changed.

## 📸 New page has no screenshots yet

`docs/administration/secret-folders.md` was written this run **without any image references**, because no capture exists for the folder view. Shots worth adding when someone next runs the pipeline:

- The folder view itself, showing breadcrumbs, an "Up to …" row, folder rows with `N secrets · N folders` counts, and the Folders/Flat toggle.
- The create-secret dialog opened from inside a folder, showing the folder-path prefix chip on the Name field.

These need new registry entries (with `depends_on: ["ui/src/pages/Secrets.tsx", "ui/src/pages/secrets/secret-path.ts"]`), not just a recapture.

## ⚠️ Registry hygiene

**38 entries have no `captured_sha` at all** and so can never be evaluated for staleness — they are invisible to this check every run. Areas affected: `company`, `dashboard`, `export-import`, `org`, `skills`, `tasks`, `work-modes` (light and dark). Recapturing them once would stamp a SHA and bring them back into the staleness system.
