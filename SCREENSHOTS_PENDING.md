# Screenshots pending recapture

Regenerated by `/sync-docs` **nightly** run. Window: parent `v2026.722.0` → `14f20be9`
(quarantine cutoff `2026-07-24T06:54:55Z`).

Captured PNGs go to a PR for human review — they are **never auto-pushed**. For a targeted
refresh:

```sh
npm run screenshots:refresh
```

Staleness below is computed from registry `depends_on` paths that changed in this window.

---

## Stale entries

### Experimental settings (4 shots, light + dark)

`ui/src/pages/InstanceExperimentalSettings.tsx` was substantially rewritten this window
(+269/-343) to render cloud-managed feature toggles as locked, marked as managed by Paperclip
(commits `0f898f95`, `216d3d26`). On a self-hosted instance the page should look unchanged, so
these captures may still be accurate — but the toggle list and card layout moved enough that
they are worth re-checking against the current build.

- `light/settings/experimental.png`
- `dark/settings/experimental.png`
- `light/experimental/recovery-preview.png`
- `dark/experimental/recovery-preview.png`

### Secrets (2 shots, light + dark)

`server/src/services/secrets.ts` changed (+77/-18) — the acting user is now threaded into
user-scoped secret resolution (`81f47e70`). The pre-dispatch check this capture illustrates is
driven by that service.

- `light/secrets/dispatch-check.png`
- `dark/secrets/dispatch-check.png`

## ⚠ Coverage gap — untracked components with rewritten copy

Commit `62e367c1` ("rewrite recovery and blocked-notice copy in plain language") changed the
user-facing strings in three notice components, and commit `148a5b11` added unblock-owner
information to the blocked notice. **No registry entry lists any of these files in
`depends_on`**, so nothing was flagged stale automatically:

- `ui/src/components/IssueBlockedNotice.tsx`
- `ui/src/components/IssueRecoveryActionCard.tsx`
- `ui/src/components/IssueAssignedBacklogNotice.tsx`

Any existing capture that happens to show a blocked task, a recovery card, or an
assigned-backlog notice is likely out of date. Worth adding registry entries with
`depends_on` pointing at these three files so the next window catches them, and worth a capture
of the blocked notice now that it names an unblock owner (see **E5** in `PENDING.md`).

## ⚠️ Registry hygiene (carried over)

**38 entries still have no `captured_sha`**, so they can never be evaluated for staleness — they
are invisible to this check on every run. Areas: `company`, `dashboard`, `export-import`, `org`,
`skills`, `tasks`, `work-modes` (light + dark). Recapturing them once would stamp a SHA and bring
them back into the staleness system.

Carried over from the previous release run and still open:

- `docs/administration/secret-folders.md` has no image references — no capture exists for the
  folder view. Needs new registry entries with
  `depends_on: ["ui/src/pages/Secrets.tsx", "ui/src/pages/secrets/secret-path.ts"]`.
- `docs/experimental/connections-apps.md` has no screenshots — the Apps surfaces are
  experimental and still moving, so captures are on hold until the UX settles.
