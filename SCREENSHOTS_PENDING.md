# Screenshots pending recapture

Regenerated by the release-mode `/sync-docs` run for parent **v2026.707.0**
(cumulative window `v2026.626.0` → `v2026.707.0`, parent tag sha
`390627b46eb333309d357004384b220ecf8a65af`).

**134 of 284** registry screenshots have a `depends_on` parent path that changed
in this window, spread across **36 routes**. They are *flagged*, not recaptured —
capture always lands in a separate PR for human review and is never auto-pushed.

Recapture with the built-in pipeline (stale only), then review the resulting PNGs:

```sh
npm run screenshots:refresh
```

## Stale routes (shot count)

Highest-churn first — these routes' underlying `ui/src/**` or `server/src/**`
sources moved in the release (env-var editor redesign, adapter label
normalization, issue-detail slimming, costs, inbox descendant status, etc.):

| Route | Shots |
|---|---|
| `/ACME/costs` | 16 |
| `/ACME/issues/{issueId}` | 14 |
| `/ACME/agents/new` | 12 |
| `/instance/settings/adapters` | 10 |
| `/ACME/agents/{agentId}/configuration` | 8 |
| `/ACME/inbox/mine` | 8 |
| `/ACME/agents/{agentId}/budget` | 4 |
| `/ACME/agents/{agentId}/runs` | 4 |
| `/ACME/inbox/requests` | 4 |
| … 27 more routes | 2 each |

## Newly-authored pages with no screenshots yet

These v2026.707.0 pages ship without captures — capture on the next screenshot pass:

- `docs/guides/projects-workflow/custom-sandbox-images.md` — environment configuration flow, custom-image build/setup states, embedded SSH terminal.

> Regenerated each run (not appended). If more `depends_on` parent paths move
> before capture happens, the stale set grows on the next run.
