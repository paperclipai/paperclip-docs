# Screenshots pending recapture

Regenerated by the **nightly** `/sync-docs` run. Cumulative window: parent release
`v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `7947308`
(24-hour cutoff `2026-07-15T08:09:25Z`; commits younger than that held back).

**246 of 298** registry screenshots have a `depends_on` parent path that changed
in this window, spread across **61 routes**. The bulk of this is the long-running
UI restyle wave in `ui/src` (component and page refactors) — most changes are
cosmetic, so treat this list as *candidates*, not a mandate to recapture all 246.

Routes marked ¹ have a dependency that changed in **this run's newly-settled delta**
(`90f85a7…7947308`, 31 commits). That set grew from 9 routes last run to **18**,
because MCP splits 6/8 and 7/8 landed the Tools, Profiles, and Apps board UI
(148 files under `ui/src/**`). Those are the highest-value recaptures right now:
**60 screenshots** across those 18 routes.

Capture with `npm run screenshots:refresh` (or `:all` after a redesign). Output PNGs
go to a human-review PR — the sync run never auto-pushes them.

## Routes

- `/ACME/activity`
- `/ACME/agents/all`
- `/ACME/agents/new`
- `/ACME/agents/{agentId}` ¹
- `/ACME/agents/{agentId}/budget` ¹
- `/ACME/agents/{agentId}/configuration` ¹
- `/ACME/agents/{agentId}/dashboard` ¹
- `/ACME/agents/{agentId}/instructions` ¹
- `/ACME/agents/{agentId}/runs` ¹
- `/ACME/agents/{agentId}/skills` ¹
- `/ACME/agents/{budgetHalfAgentId}/budget`
- `/ACME/agents/{budgetMaxAgentId}/budget`
- `/ACME/agents/{budgetWarnAgentId}/budget`
- `/ACME/agents/{httpAgentId}/configuration`
- `/ACME/agents/{longRunnerAgentId}` ¹
- `/ACME/agents/{longRunnerAgentId}/runs` ¹
- `/ACME/agents/{runnerAgentId}/configuration`
- `/ACME/agents/{runnerAgentId}/runs/{runnerRunId}` ¹
- `/ACME/approvals/pending`
- `/ACME/approvals/{approvedApprovalId}?resolved=approved`
- `/ACME/approvals/{hireApprovalId}`
- `/ACME/approvals/{strategyApprovalId}`
- `/ACME/company/export`
- `/ACME/company/import`
- `/ACME/company/settings`
- `/ACME/company/settings/access`
- `/ACME/company/settings/invites`
- `/ACME/company/settings/secrets` ¹
- `/ACME/costs`
- `/ACME/dashboard` ¹
- `/ACME/execution-workspaces/{executionWorkspaceId}/configuration`
- `/ACME/execution-workspaces/{executionWorkspaceId}/issues`
- `/ACME/execution-workspaces/{executionWorkspaceId}/runtime-logs`
- `/ACME/execution-workspaces/{executionWorkspaceId}/services`
- `/ACME/inbox/mine`
- `/ACME/inbox/recent`
- `/ACME/inbox/requests`
- `/ACME/inbox/unread`
- `/ACME/issues`
- `/ACME/issues/{issueId}` ¹
- `/ACME/org`
- `/ACME/projects`
- `/ACME/projects/{projectId}/budget`
- `/ACME/projects/{projectId}/configuration`
- `/ACME/projects/{projectId}/issues`
- `/ACME/projects/{projectId}/overview`
- `/ACME/projects/{projectId}/workspaces`
- `/ACME/routines`
- `/ACME/routines/{routineId}`
- `/ACME/skills` ¹
- `/ACME/skills/{skillId}` ¹
- `/ACME/timeline`
- `/instance/settings/access`
- `/instance/settings/adapters`
- `/instance/settings/experimental` ¹
- `/instance/settings/general`
- `/instance/settings/heartbeats`
- `/instance/settings/plugins` ¹
- `/instance/settings/profile`
- `/{emptyPrefix}/agents/all`
- `/{emptyPrefix}/dashboard` ¹
