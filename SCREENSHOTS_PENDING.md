# Screenshots pending recapture

Regenerated by `/sync-docs` **nightly** run. Window: parent `v2026.722.0` → `2568bdec`
(quarantine cutoff `2026-07-27T08:57:42Z`).

Captured PNGs go to a PR for human review — they are **never auto-pushed**. For a targeted
refresh:

```sh
npm run screenshots:refresh
```

Staleness below is computed from registry `depends_on` paths that changed in this window.

**110 of 310 registry entries flagged, driven by 14 changed parent paths.** Most of that number
is noise: the archived-projects work (`56487002`, `2002c4ff`) swapped `queryKeys.projects.list`
for `queryKeys.projects.all` — or added an explicit `{ includeArchived: true }` — across a dozen
pages that render exactly the same pixels afterwards. Those are listed last, under **Low risk**.
The sections above them are the ones actually worth a recapture.

---

## Worth recapturing

### Routines list (2 shots, light + dark)

`ui/src/pages/Routines.tsx` (`c9881223`, +42/-4) now renders folder groups as inline sections in
the main pane, with built-in routines split into their own section after the custom folders. The
current captures show the previous flat rendering.

- `light/routines/list.png`
- `dark/routines/list.png`

### Routine detail (6 shots, light + dark)

`ui/src/pages/RoutineDetail.tsx` (`30ff3d7c`, +20/-9) gained the activity-gate controls
(`activityGatePolicy` / `activityGateScope`) documented under **N3** in `PENDING.md`. The editor
panel has new fields these captures do not show.

- `light/routines/detail.png` · `dark/routines/detail.png`
- `light/routines/cron-picker.png` · `dark/routines/cron-picker.png`
- `light/routines/run-history.png` · `dark/routines/run-history.png`

### Inbox rows (22 shots, light + dark)

`ui/src/pages/Inbox.tsx` (`4c55f0d8`) stopped passing external-object summaries into inbox task
rows, so the external-object markers that appeared alongside the core task information are gone.
Filtering behaviour is unchanged — only the row marker was removed. Any capture showing an inbox
row with an external-object marker is now wrong.

- `light/issues/inbox.png` · `dark/issues/inbox.png`
- `light/issues/inbox-unread.png` · `dark/issues/inbox-unread.png`
- `light/issues/inbox-requests.png` · `dark/issues/inbox-requests.png`
- `light/issues/my-issues.png` · `dark/issues/my-issues.png`
- `light/tasks/inbox-view.png` · `dark/tasks/inbox-view.png`
- `light/tasks/inbox-view-with-filters.png` · `dark/tasks/inbox-view-with-filters.png`
- `light/tasks/inbox-newly-created-tasks.png` · `dark/tasks/inbox-newly-created-tasks.png`
- `light/tasks/new-task-form-empty.png` · `dark/tasks/new-task-form-empty.png`
- `light/tasks/new-task-form-filled.png` · `dark/tasks/new-task-form-filled.png`
- `light/tasks/task-done-status.png` · `dark/tasks/task-done-status.png`
- `light/company/join-requests.png` · `dark/company/join-requests.png`

### Experimental settings (4 shots, light + dark)

The Experimental settings page gained a **Status Cards** card this window (**N1**). The toggle
list on that page is data-driven from `packages/shared/src/feature-catalog.ts`, so the new entry
appears without any edit to the page component itself.

- `light/settings/experimental.png` · `dark/settings/experimental.png`
- `light/experimental/recovery-preview.png` · `dark/experimental/recovery-preview.png`

### Projects list (2 shots, light + dark)

`ui/src/pages/Projects.tsx` — archived projects no longer come back from the default project list
route (**N4**). If the seeded demo company has an archived project, the list is now shorter than
what the current capture shows.

- `light/projects/list.png` · `dark/projects/list.png`

### Task watchdogs (2 shots, light + dark)

`server/src/services/task-watchdogs.ts` (`c481be44`, +174/-12) — the scheduler now skips a wake
when the stopped-state snapshot is unchanged (**N7**), so a watchdog thread shows fewer repeated
wake entries than it used to.

- `light/watchdogs/watchdog-thread-outcome.png` · `dark/watchdogs/watchdog-thread-outcome.png`

### Secrets (2 shots, light + dark) — carried over

`server/src/services/secrets.ts` is still in the cumulative window from **E8** (acting user
threaded into user-scoped resolution). Unchanged since the last run; still not recaptured.

- `light/secrets/dispatch-check.png` · `dark/secrets/dispatch-check.png`

## Low risk — flagged but almost certainly unchanged

These entries were flagged only because their `depends_on` page took the archived-projects
query-key refactor (`56487002`, `2002c4ff`). Dashboard and GoalDetail explicitly pass
`{ includeArchived: true }` to preserve their previous contents, and the rest are cache
invalidation keys with no render effect.

| Depends-on path | Shots | Change |
| --- | --- | --- |
| `ui/src/pages/IssueDetail.tsx` | 24 | query-key plumbing |
| `ui/src/pages/Costs.tsx` | 16 | `projects.list` → `projects.all` invalidation |
| `ui/src/pages/Dashboard.tsx` | 14 | explicit `includeArchived: true`, same result |
| `ui/src/pages/ProjectDetail.tsx` | 10 | `projects.list` → `projects.all` invalidation |
| `ui/src/pages/CompanyExport.tsx` | 4 | query-key plumbing |
| `ui/src/pages/GoalDetail.tsx` | 2 | explicit `includeArchived: true`, same result |
| `ui/src/components/NewIssueDialog.tsx` | 2 | query-key plumbing |

Recapture them if you are already doing a full refresh; skip them if you are being targeted.

## ⚠ Coverage gap — Status Cards has no registry entries at all

`grep StatusCards docs/user-guides/screenshots/registry.json` returns nothing. The whole new
surface ships undocumented visually:

- `ui/src/pages/StatusCards/index.tsx` — the board
- `ui/src/pages/StatusCards/CreateStatusCardDialog.tsx` — the create flow
- `ui/src/pages/StatusCards/StatusCardSettingsForm.tsx` — refresh policy, active hours, token cap
- `ui/src/pages/StatusCards/StatusCardDetailDrawer.tsx` — the generated summary and its history
- `ui/src/pages/StatusCards/StatusCardTile.tsx` — the card tile and its state badges

`docs/experimental/status-cards.md` (new this run) has no images because none exist. Worth adding
registry entries with `depends_on` pointing at these files, plus a route, once the feature is
worth capturing.

## ⚠ Coverage gap — `feature-catalog.ts` is not a tracked dependency

No registry entry lists `packages/shared/src/feature-catalog.ts` in `depends_on`, but that file
is what determines which cards render on the Experimental settings page. This window happened to
flag `settings/experimental.png` anyway (because `InstanceExperimentalSettings.tsx` is still in
the cumulative window from **E1**), but a future window that adds a feature flag *without*
touching the page component would silently miss it. Add it to the `depends_on` of both
`experimental.png` entries.

## ⚠ Coverage gap — untracked components with rewritten copy (carried over)

Still open from the previous run. Commit `62e367c1` changed user-facing strings in three notice
components and `148a5b11` added unblock-owner information to the blocked notice, but **no registry
entry lists any of these files in `depends_on`**:

- `ui/src/components/IssueBlockedNotice.tsx`
- `ui/src/components/IssueRecoveryActionCard.tsx`
- `ui/src/components/IssueAssignedBacklogNotice.tsx`

## ⚠️ Registry hygiene (carried over)

**38 entries still have no `captured_sha`**, so they can never be evaluated for staleness — they
are invisible to this check on every run. Areas: `company`, `dashboard`, `export-import`, `org`,
`skills`, `tasks`, `work-modes` (light + dark). Recapturing them once would stamp a SHA and bring
them back into the staleness system.

Also still open:

- `docs/administration/secret-folders.md` has no image references — no capture exists for the
  folder view. Needs new registry entries with
  `depends_on: ["ui/src/pages/Secrets.tsx", "ui/src/pages/secrets/secret-path.ts"]`.
- `docs/experimental/connections-apps.md` has no screenshots — the Apps surfaces are
  experimental and still moving, so captures are on hold until the UX settles.
