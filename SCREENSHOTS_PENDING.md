# SCREENSHOTS_PENDING — nightly docs sync

Regenerated by `/sync-docs` each run. Lists screenshots whose `depends_on` parent paths changed in
the current diff window. Recapture with `npm run screenshots:refresh` (output PNGs land in a PR for
human review — never auto-pushed).

**36 of 310 screenshots are flagged stale against this run's delta (`2ab797d → 8142e54`).** The
handful that actually matter for the pages authored this run are called out first; the rest are
broad task-detail churn from `ui/src/pages/IssueDetail.tsx`.

## Directly relevant to this run's edits — recapture first

These back the Activity/Audit page merge (cluster B) and the cross-task-write surfaces (cluster A).

| Screenshot | Changed dependency |
| --- | --- |
| `light/activity/activity-log-full.png` | `ui/src/pages/Activity.tsx` |
| `dark/activity/activity-log-full.png` | `ui/src/pages/Activity.tsx` |
| `light/activity/activity-filters.png` | `ui/src/pages/Activity.tsx` |
| `dark/activity/activity-filters.png` | `ui/src/pages/Activity.tsx` |
| `light/activity/activity-filtered-by-agent.png` | `ui/src/pages/Activity.tsx` |
| `dark/activity/activity-filtered-by-agent.png` | `ui/src/pages/Activity.tsx` |
| `light/issues/detail-activity.png` | `ui/src/pages/IssueDetail.tsx` |
| `dark/issues/detail-activity.png` | `ui/src/pages/IssueDetail.tsx` |
| `light/tasks/task-detail-with-comments.png` | `ui/src/pages/IssueDetail.tsx` |
| `dark/tasks/task-detail-with-comments.png` | `ui/src/pages/IssueDetail.tsx` |
| `light/interactions/checkbox-confirmation-card.png` | `ui/src/pages/IssueDetail.tsx` |
| `dark/interactions/checkbox-confirmation-card.png` | `ui/src/pages/IssueDetail.tsx` |

> **Registry note:** the `activity/*` entries still list `depends_on: ui/src/pages/Activity.tsx`.
> Upstream, the unified page moved to `ui/src/pages/audit/CompanyActivity.tsx` (with
> `ui/src/pages/audit/CompanyAudit.tsx` removed). Update those `depends_on` paths when the
> screenshots are recaptured, or they will stop tracking the real source.

## Broader task-detail churn (this run's delta)

The remaining stale entries all hang off `ui/src/pages/IssueDetail.tsx` (chat, sidebar, list,
work-modes, watchdogs) or `ui/src/pages/CompanySettings.tsx` / `SidebarCompanyMenu.tsx`. They are
task-detail polish, not new surfaces this run documents — refresh them opportunistically:

`{light,dark}/issues/detail-chat.png`, `{light,dark}/issues/detail-sidebar.png`,
`{light,dark}/issues/list.png`, `{light,dark}/tasks/task-detail-in-progress.png`,
`{light,dark}/tasks/comment-input-box.png`, `{light,dark}/watchdogs/watchdog-thread-outcome.png`,
`{light,dark}/work-modes/{ask-mode-answer,plan-mode-plan,standard-mode-run}.png`,
`{light,dark}/company/{settings,company-saved}.png`,
`{light,dark}/onboarding/sidebar-new-company-button.png`.

A full recapture across a release cycle of UI churn is a `npm run screenshots:refresh:all`
decision, not a per-entry chase.
