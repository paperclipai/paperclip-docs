# SCREENSHOTS_PENDING — nightly docs sync

Regenerated by `/sync-docs` each run. Lists screenshots whose `depends_on` parent paths changed in
the current diff window (`v2026.722.0` → `8540ce29`). Recapture with `npm run screenshots:refresh`
(output PNGs land in a PR for human review — never auto-pushed).

**154 of 310 screenshots are flagged stale.** That number is inflated because this window spans a
whole release cycle of UI churn, not just this run's delta. Recapturing everything is a
`npm run screenshots:refresh:all` decision, not a per-entry chase. The entries below are split into
what's **relevant to the pages changed this run** vs. the **broad churn** behind the rest.

## Blocked — dependency removed upstream

These two can never be recaptured: `ui/src/pages/CloudUpstream.tsx` is confirmed `removed` in this
window (Cloud Sync deleted upstream — see R1 in `PENDING.md`). Delete them from the registry when
the cloud doc pages are resolved at release time.

| Screenshot | Removed dependency |
| --- | --- |
| `dark/experimental/cloud-upstream.png` | `ui/src/pages/CloudUpstream.tsx` |
| `light/experimental/cloud-upstream.png` | `ui/src/pages/CloudUpstream.tsx` |

## Relevant to this run's doc changes (recapture these)

Tied to **P6 — cache-adjusted run cost** (`docs/guides/day-to-day/costs.md`):

| Count | Changed dependency |
| --- | --- |
| 16 | `ui/src/pages/Costs.tsx` — `costs/billers`, `costs/budgets`, `costs/company-budget-field`, `costs/costs-dashboard-overview`, and the rest of `*/costs/**` |

Tied to **P7 — Issue Plans and Artifacts tabs** (`docs/guides/day-to-day/issues.md`,
`docs/guides/day-to-day/artifacts.md`):

| Count | Changed dependency |
| --- | --- |
| 24 | `ui/src/pages/IssueDetail.tsx` — includes `issues/detail-sidebar`, `issues/detail-chat`, `issues/detail-activity` |
| 2 | `ui/src/components/IssueThreadInteractionCard.tsx` — `interactions/checkbox-confirmation-card` |

Tied to **P4 — chat-style task view** (`docs/experimental/task-chat.md`). The flag is off by default,
so these captures still show the classic page — recapture only confirms the default view is
unchanged:

| Count | Changed dependency |
| --- | --- |
| 4 | `ui/src/components/IssueChatThread.tsx` — `work-modes/ask-mode-answer`, `work-modes/standard-mode-run` |
| 2 | `ui/src/components/transcript/RunTranscriptView.tsx` — `agents/run-transcript-view` |

Tied to the inbox/attention behaviour changes behind **P2 — Decisions v1**:

| Count | Changed dependency |
| --- | --- |
| 22 | `ui/src/pages/Inbox.tsx` — `issues/inbox`, `issues/inbox-unread`, `issues/inbox-requests`, `company/join-requests` |

## Broad churn (batch decision, not per-entry)

The remaining flagged entries come from release-cycle-wide UI churn already applied in earlier runs;
none map to a doc edit made this run.

| Count | Changed dependency |
| --- | --- |
| 24 | `ui/src/pages/AgentDetail.tsx` |
| 14 | `ui/src/pages/Dashboard.tsx` |
| 10 | `ui/src/pages/ProjectDetail.tsx` |
| 6 | `ui/src/pages/RoutineDetail.tsx` |
| 4 | `ui/src/pages/CompanySettings.tsx` |
| 4 | `ui/src/pages/CompanyExport.tsx` |
| 4 | `ui/src/pages/CompanyImport.tsx` |
| 4 | `ui/src/pages/InstanceExperimentalSettings.tsx` |
| 4 | `ui/src/pages/Secrets.tsx` |
| 4 | `ui/src/pages/Timeline.tsx` |
| 4 | `server/src/services/work-timeline.ts` |
| 2 each | `CompanyEnvironments.tsx`, `GoalDetail.tsx`, `Projects.tsx`, `Routines.tsx`, `NewIssueDialog.tsx`, `api/workTimeline.ts`, `services/secrets.ts`, `services/task-watchdogs.ts` |
