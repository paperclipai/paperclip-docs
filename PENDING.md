# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the current cumulative view from the last parent release; it retains already-applied work for context and only treats the newly-settled delta as authoring scope.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` HEAD `f12bb27`. The compare helper saw **195 commits / 1522 changed files** with **0 truncated leaves**. Every commit in the window predates the 24-hour quarantine cutoff (newest is `2026-07-18T19:39:37Z`), so nothing was held back this run.

Newly settled since the prior run: `7947308…f12bb27` (**60 commits / 397 files**), headlined by four new REST surfaces and the summary-slot feature.

## ✅ Applied in earlier nightly drafts

- **Cases API** — `docs/reference/api/cases.md`.
- **Built-in Agents API** — `docs/reference/api/built-in-agents.md`.
- **Human permission keys** — `docs/administration/roles-and-permissions.md`.
- **Codex `gpt-5.4-mini`** — `docs/reference/adapters/codex.md`.
- **Decisions, ACP, inbox, and interaction contracts** — API, guide, adapter, and CLI pages updated in PR #57.
- **Redundant issue re-wake throttling** (`5dff526`) — `docs/how-to/debug-stuck-heartbeat.md`.
- **`costStatus` on cost events** (`efcce9c`) — `docs/reference/api/costs.md`.
- **The six `tools:*` permission keys** — `docs/administration/roles-and-permissions.md`.
- **`PAPERCLIP_TOOL_ACTION_SIGNING_SECRET`** — `docs/reference/deploy/environment-variables.md`.

## ✍️ Newly settled, applied

- **Folders API** — new page `docs/reference/api/folders.md`. `server/src/routes/folders.ts` mounts under `/api` and covers list, create, rename/update, move-item, move-folder, and delete. Documented against the real zod schemas; `MAX_FOLDER_DEPTH = 4` and the reserved root slugs (`bundled`, `my`, `projects`) are service constants, and `path`/`depth` are computed in `buildFolderViews` rather than stored columns. Verification: 7/7 claims confirmed.

- **Decision Training API** — new page `docs/reference/api/decision-training.md`. Covers create, preview, list, the `export.jsonl` endpoint, get, update-notes, and delete. Source kinds are exactly `interaction`, `approval`, `execution_decision`. The retention policy constant `scrub_deleted_comments_v1` was confirmed directly in `packages/shared/src/types/decision-training.ts`. Verification: 7/7 claims confirmed.

- **Company Skill Policy API** — new page `docs/reference/api/company-skill-policy.md`. Get / put / delete / evaluate under `/api/companies/{companyId}/skill-policy`. Note the policy routes wrap zod failures themselves into `422 skill_policy_validation_failed` rather than the usual 400. Verification: 4/4 claims confirmed.

- **Inbox Agent Policy API** — new page `docs/reference/api/inbox-agent-policy.md`. Documents both the self routes (`/users/me/inbox-agent-policy`) and the admin-on-behalf routes (`/users/{userId}/inbox-agent-policy`). Verification: 4/4 claims confirmed.

- **`inbox:manage` permission key** (drift fix) — `docs/administration/roles-and-permissions.md`. `PERMISSION_KEYS` is now **twenty** keys; the count carrying a role default stays at **8**, so the explicit-only set goes from eleven to twelve. The important subtlety, confirmed in `server/src/services/authorization.ts`: `inbox:manage` is **not** checked by the inbox-agent-policy *routes* (those check `users:manage_permissions`). It gates an **agent acting on an inbox** — an agent may work its own responsible user's inbox without it, but cross-user inbox action needs an explicit scoped grant, and the scope must cover the target user. Agents on the low-trust review preset are denied it regardless of grant.

- **`summarize-status` bundled skill** — `docs/reference/skills.md`. New `paperclip-operations`-category bundled skill, `defaultInstall: false`, recommended for `general` and `manager`. Added to the bundled examples row and given a short paragraph.

## 🚧 Newly settled, surfaced but NOT applied

- **Summary slots** — `packages/db/src/schema/summary_slots.ts`, `server/src/routes/summary-slots.ts` (mounted `api.use(summarySlotRoutes(db))`), and `ui/src/api/summarySlots.ts`. This is the feature the new `summarize-status` skill writes into: a scope (project, workspaces overview, or a single project workspace) carries a summary slot that accumulates Markdown **revisions**. It deserves its own API page plus a short guide section on where the summary renders next to the board — not a one-line mention. Currently only referenced obliquely via the skill paragraph in `docs/reference/skills.md`.

- **The Tool access / MCP gateway / Smoke Lab REST surface (~60 endpoints)** — still outstanding from the prior run. `server/src/routes/tool-access.ts`, `tool-gateway.ts`, `smoke-lab.ts`. Groundwork retained:
  - **Mount prefixes differ, and this is a trap.** `toolAccessRoutes`, `toolGatewayRoutes`, and `smokeLabRoutes` mount on the `api` router (`app.use("/api", api)`), so they are `/api/...`. But `mcpGatewayProtocolRoutes(toolGateway)` mounts on `app` **directly** — so `GET|POST /mcp/gateways/{gatewayPublicId}` has **no** `/api` prefix.
  - `tool-gateway.ts` is **not** exported from `server/src/routes/index.ts`; it is imported straight into `app.ts`.
  - Vocabulary in `packages/shared/src/constants.ts`: `TOOL_APPLICATION_TYPES` (`mcp_http`, `mcp_stdio`, `paperclip_plugin`, `a2a`), `TOOL_APPLICATION_STATUSES` (`draft`, `active`, `disabled`, `archived`), `TOOL_CONNECTION_HEALTH_STATUSES`, `TOOL_MCP_GATEWAY_DEFAULT_PROFILE_MODES`.

- **Tools, Profiles, and Apps board UI** — carried over, plus this window's UI work. New end-user surfaces needing guide coverage and screenshots.

- **Decision Training UI** — `ui/src/components/DecisionTrainingDrawer.tsx` and `ui/src/api/decisionTraining.ts`. The API is now documented; the board-side drawer that produces the examples is not, and needs a guide page with screenshots.

- **Folders UI** — `ui/src/components/folders/*` (`SkillFolderTree`, `FolderControls`, `skill-folder-tree.ts`). The API is documented; the skill-library folder tree the user actually clicks is not.

## ⚠ Reconcile

- **`acpx_local` retired to a live tombstone — rewrite `acpx-local.md`, do NOT delete it.**

  `scripts/sync/detect-renames.mjs` reported `packages/adapters/acpx-local` under `removed_dirs_no_match`, which reads as a plain deletion. **That verdict is wrong.** Verified against parent `eedc7dd` ("Make ACP the default engine for local adapters", #9238):

  1. **The runtime moved, it did not vanish.** `packages/adapters/acpx-local/` → `packages/adapter-utils/src/acpx-engine/` (module `@paperclipai/acpx-engine`), now imported by `claude-local`, `codex-local`, and `gemini-local`. ACP stopped being a separate *adapter choice* and became an *execution capability* of each harness adapter.
  2. **The `acpx_local` adapter type is still registered, deliberately.** `server/src/adapters/registry.ts:215` keeps an `acpxLocalAdapter` whose `execute` only emits a retirement message. Its own comment: *"Paperclip keeps this tombstone registered so stale `acpx_local` rows fail clearly instead of falling back to the process adapter."* The type is gone from `AGENT_ADAPTER_TYPES` (so it is unselectable for new agents) but still resolvable at runtime.
  3. **Existing rows were migrated.** `packages/db/src/migrations/0136_acpx_default_engine_migration.sql` rewrites `acpx_local` agents to `claude_local` or `codex_local` (branching on `adapter_config->>'agent'`) with `engine: 'acp'`, and consolidates the Codex reasoning-effort fields onto `modelReasoningEffort`.

  **Therefore: keep the page and convert it to a tombstone.** Deleting it would be a mistake — a user whose agent still carries a stale row sees the literal string `acpx_local` in a runtime error and will search the docs for exactly that. The page should state the adapter is retired, name the replacement (`claude_local` / `codex_local` with `adapterConfig.engine="acp"`), and say that existing agents were migrated automatically by `0136`. Drop the `paperclip_version: v2026.525.0` frontmatter or bump it. Keep the nav entry (`site/content.json:725`).

  **Helper gap worth fixing.** `detect-renames.mjs` matches top-level directories under watched roots, so it cannot see a move that crosses a package boundary into a nested path (`packages/adapters/<x>` → `packages/adapter-utils/src/<x>`). Any future cross-package extraction will be misreported the same way. Consider matching on file-content similarity, or at minimum scanning for the removed dir's basename appearing as a new nested path elsewhere in the window.

## ↻ Renames detected

None. `detect-renames.mjs` returned `renames: []` and `added_dirs_genuinely_new: []` for this window.

## ⚠ Verification

All six applied edits passed `verify-edit.mjs` against parent `master` (`f12bb27`) with **zero** unverified and **zero** suspicious records. `roles-and-permissions.md` extracts 0 machine-checkable claims (the helper does not parse permission-key tables), so its facts were confirmed by hand against `packages/shared/src/constants.ts` (`PERMISSION_KEYS`, 20 entries), `server/src/services/company-member-roles.ts` (`grantsForHumanRole`), and `server/src/services/authorization.ts` (the `inbox:manage` enforcement branches).

## 🩺 Housekeeping

- **Stale `last_seen_parent_sha`.** The value stored by the prior run, `992389480a243b97bda214227e0767eb8c3672af`, **no longer resolves** in `paperclipai/paperclip` — `npm run sync:check` fails its drift step on it. The commit was most likely rebased away or GC'd upstream. This run's state update to `f12bb27` clears it. The field is informational only, so nothing else was affected; the Phase 1.5 drift check ran cleanly against `master` explicitly.
- **Orphan nav files** — `docs/reference/cli/commands.md` and `docs/reference/cli/control-plane-commands.md` remain intentional redirect anchors, not missed registrations. Expected warning.

## ↻ Newly settled, context-only

- **Codex auth precedence and copyback** — `packages/adapters/codex-local/src/server/auth-precedence.ts`, `codex-auth-copyback.ts`, `codex-auth-merge-scripts.ts` and friends. Internal auth-resolution behaviour; no documented adapter config field changed.
- **Claude adapter parse changes** — `packages/adapters/claude-local/src/server/parse.ts`. Transcript parsing internals.
- **Hot restart** — `server/src/services/hot-restart.ts` (new). No documented contract or env var yet.
- **Company search extract** — `server/src/services/company-search-extract.ts` (new). Internal extraction split out of `company-search.ts`.
- **Smoke Lab fixture demo servers** — `packages/google-sheets-mcp-server` and `packages/kv-demo-mcp-server` match the env-vars watcher via `packages/**/config.ts`, but their config is fixture-local, not instance configuration. Not documented.

## 📸 Screenshots

**248 of 298** registry entries are stale — see `SCREENSHOTS_PENDING.md` for the per-area breakdown. The cumulative window touched **614** files under `ui/src/**`, so this is closer to a full-surface refresh than a set of targeted recaptures. Consider `npm run screenshots:refresh:all` rather than the stale-only pass. Output PNGs go to a PR for human review; the skill never pushes them.
