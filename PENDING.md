# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the current cumulative view from the last parent release; it retains already-applied work for context and only treats the newly-settled delta as authoring scope.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `c36f1a4` (24-hour cutoff `2026-07-13T08:01:56Z`). The compare helper saw **95 commits / 906 changed files** with **0 truncated leaves**. Newly settled since the prior run: `9e7e84e…c36f1a4` (**5 commits / 25 files**).

## ✅ Applied in earlier nightly drafts

- **Cases API** — `docs/reference/api/cases.md`.
- **Built-in Agents API** — `docs/reference/api/built-in-agents.md`.
- **Human permission keys** — `docs/administration/roles-and-permissions.md`.
- **Codex `gpt-5.4-mini`** — `docs/reference/adapters/codex.md`.
- **Decisions, ACP, inbox, and interaction contracts** — the API, guide, adapter, and CLI pages updated in PR #57.

## ✍️ Newly settled, applied

- **Redundant issue re-wake throttling** (`5dff526`) — `docs/how-to/debug-stuck-heartbeat.md` now explains when Paperclip records an `issue_rewake_throttled` skip and how to resume useful work without waiting on a no-op loop.

## ↻ Newly settled, context-only

- **ACP-lane spend telemetry** (`9e7e84e`) now attributes ACP usage and cost to the configured provider/biller in the internal cost ledger. It adds no adapter configuration, route, or user-facing data contract.
- **Fresh-heartbeat execution-contract injection** (`c8253e3`) refreshes adapter execution context between runs. It is an internal adapter-runtime correction with no configuration change.
- **Decisions queue performance and mobile layout** (`8a0db22`, `c36f1a4`) improve rendering and readability without adding a new workflow or control. No screenshot registry entry depends on the changed files.
- **`acpx_local` removal** — rename detection still reports `packages/adapters/acpx-local` removed with no target. Its existing page remains the retirement/migration reference; a human should decide whether it should become redirect-only.

## ⚠ Drift — surfaced, not applied

No candidates. The elevated canonical-parent drift check against `master` at `90f85a7` completed cleanly.

## 🕒 Quarantined for the next run

- `8775bde` — runtime asset build-gap guard.
- `634ae12` — UI status-color and inbox-badge refinements.
- `0e21a27` — Hermes/process-adapter PID persistence.
- `b49d178` — experiments auto-recovery dialog fix.
- `ce7dedf` — CI shard balancing.
- `efcce9c` — adapter CLI usage telemetry.
- `6f20460` — unreleased-build source-SHA UI.
- `6a36ae4` — stable-release dry-run notes gate.
- `90f85a7` — telemetry proposal extractor.

## 📸 Screenshots

- **246 of 298** registry screenshots across **61 routes** remain stale candidates in the cumulative window. The exact newly-settled files do not add a matching registry dependency. They are flagged in `SCREENSHOTS_PENDING.md`, not recaptured; capture output belongs in a separate human-review PR.

## Verification

The authored heartbeat guidance was checked by hand against canonical parent `server/src/services/issue-rewake-throttle.ts` and `server/src/services/heartbeat.ts` at `c36f1a4afd91e4ddf0e5c7224b288ce722c7404f`. The `issue_rewake_throttled` reason, two-run threshold, two-minute base cooldown, 30-minute cap, new-comment/new-input and explicit-resume bypasses, and direct server-side recovery retries all match the source verbatim.

_Manifest hash: `8227f5f1631c1e8d86a0778339774a38b4a6ae11fe85a1741997231e976f8128`_
