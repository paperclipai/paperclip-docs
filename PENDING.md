# PENDING — nightly docs sync

_Regenerated by `/sync-docs` (nightly mode) on 2026-08-10. Overwritten each run from the current change manifest — do not hand-edit._

- **Window:** parent `v2026.722.0` (base release) → quarantine boundary `19be4cf9` (newest commit older than 24h). Parent HEAD is `6a4e2e1`; the two newest commits (`6a4e2e1`, `ebf2b8ff`) are held back by the 24h quarantine.
- **New since last run** (`814cb33` → `19be4cf9`): 34 commits, 262 files.
- **Drift (Phase 1.5):** none — 0 records across 10 parent paths, 59 CLI commands, 69 env vars, 430 REST routes, 19 permission keys.
- **Reconcile (Phase 3.5):** none — the reverted onboarding-port feature and its revert both fall inside the window (net-zero); no dangling doc references found.

## Applied this run (PR-tier drafts against `nightly`)

1. **Agent skill-assignment `mode` (breaking).** `POST /api/agents/{id}/skills/sync` and CLI `skills agent sync` now require a merge mode — `add` / `remove` / `replace` — where the operation used to be an unconditional full replace.
   - Parent: `cli/src/commands/client/skills.ts`, `server/src/routes/agents.ts` (commit "feat(skills): require explicit merge modes").
   - Docs: `docs/reference/cli/skills.md`, `docs/reference/api/agents.md`, `docs/how-to/write-a-company-skill.md`, `docs/guides/org/skills.md`.

2. **New optional catalog skill `prepare-mcp-integration`.** First skill in a new `software-development` catalog category.
   - Parent: `packages/skills-catalog/catalog/optional/software-development/prepare-mcp-integration/SKILL.md` (commit "feat(skills): add MCP integration preparation skill").
   - Docs: new `docs/reference/skills/optional/software-development/prepare-mcp-integration.md`; `docs/reference/skills/optional.md`; `docs/reference/skills.md`; nav in `site/content.json`.

3. **Plugin SDK: live execute output + `bypassSession`.** New `ctx.execution.log(stream, chunk)` client on `PluginContext` and an optional `bypassSession?` param on `PluginEnvironmentExecuteParams`.
   - Parent: `packages/plugins/sdk/src/types.ts`, `packages/plugins/sdk/src/protocol.ts`.
   - Docs: `docs/reference/plugins/sdk.md`.

## Triaged, no doc action this run

- **`server/src/services/tool-access.ts` (+799)** and route `tool-access.ts` (+1): internal logic; the `tool_access` schema change only dropped an internal DB uniqueIndex (`tool_connections_company_name_uq`) — no documented surface affected.
- **Recovery-notice humanization** (`server/src/services/recovery/*`): UX wording refinement; no doc quotes the old notice text, so no required change. Watch `docs/how-to/debug-stuck-heartbeat.md` / `docs/guides/day-to-day/blocked-inbox.md` on the next screenshot pass.
- **CLI `configure` / `onboard` invalid-config backup** (config now backed up and confirmation-gated on repair): minor UX; no reference page currently documents the repair flow.
- **`web-ui` (104 files):** mostly tests and internal refactors → surfaced as screenshot staleness (see `SCREENSHOTS_PENDING.md`), not prose.
- **`db-migrations` (context-only):** correlate with the schema/tool-access changes above; no direct prose.

## Screenshots

142 screenshots flagged stale (UI-heavy window) — see `SCREENSHOTS_PENDING.md`. Recapture with `npm run screenshots:refresh`; PNGs land in a PR for human review, never auto-pushed.
