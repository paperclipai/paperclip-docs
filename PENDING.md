# PENDING — nightly docs sync

_Regenerated by `/sync-docs` (nightly mode) on 2026-08-11. Overwritten each run from the current change manifest — do not hand-edit._

- **Window:** parent `v2026.722.0` (base release) → quarantine boundary `6a4e2e1` (newest commit older than 24h). Parent HEAD is `66575fe`; the 13 newest commits (`f917378`…`66575fe`, all dated 2026-08-10T19:16Z or later) are held back by the 24h quarantine (cutoff `2026-08-10T09:07Z`).
- **New since last run** (`19be4cf` → `6a4e2e1`): 2 commits, 4 files. Both previously quarantined; now settled.
- **Drift (Phase 1.5):** none — 0 records across all scanned parent paths, CLI commands, env vars, REST routes, and permission keys.
- **Reconcile (Phase 3.5):** none — both newly-settled commits are pure additions; no previously-documented surface disappeared from the cumulative diff.

## Applied this run

None. The two commits that settled past quarantine this cycle are internal-only:

- **`fix(routes): return 409 for routine checkout conflicts`** (`server/src/routes/issues.ts`, new `server/src/db-errors.ts` + test). The agent-only issue-checkout endpoint now maps the `issues_open_routine_execution_uq` unique-violation to a `409 { error: "Another execution for this routine is already in progress" }` instead of surfacing a raw 500. This is an internal robustness fix on an agent-run endpoint — not a documented public API route — so no reference page changes.
- **`fix(server): persist worktree runtime port when ambient PORT does not match`** (`server/src/worktree-config.ts`). Internal worktree runtime-port persistence; no user-visible surface.

## Triaged, no doc action this run

- Everything else in the cumulative window (`v2026.722.0` → `6a4e2e1`) was already authored in prior nightly drafts (#75–#88, up to parent `19be4cf`) and merged onto `nightly`. This run adds no new doc-relevant surface on top of that.
- **`web-ui` churn:** surfaced as screenshot staleness (see `SCREENSHOTS_PENDING.md`), not prose.
- **`db-migrations` (context-only):** no direct prose.

## Screenshots

198 captured screenshots are stale against their own capture shas, and 38 have never been captured — see `SCREENSHOTS_PENDING.md`. Recapture with `npm run screenshots:refresh`; PNGs land in a PR for human review, never auto-pushed.
