# PENDING — nightly docs sync

Regenerated by `/sync-docs` (nightly mode). Cumulative window: parent release `v2026.626.0` (`4c6c0c6`) → quarantine cutoff `f019f54b`. Parent `master` HEAD at run time was `a8f0ebaa`; everything committed after `2026-06-30T08:48:11Z` (now − 24h) is held in quarantine below.

This file is overwritten each run — it always reflects the current run, not an append log.

## Applied this run (committed to `nightly`)

| Parent commit | Surface | Doc target | Outcome |
|---|---|---|---|
| `5e3d6e36` [codex] Preserve plan review context in agent wakes (#8649) | `planReviewContext` field added to `GET /api/issues/{issueId}/heartbeat-context` | `docs/reference/api/issues.md` | Appended a bullet to the Heartbeat Context payload list describing the new plan-review feedback field. Verified against parent (`verify-edit.mjs`: 0 unverified / 0 suspicious). |

## Deferred — needs a human decision (not auto-authored)

| Parent commit | Surface | Why deferred |
|---|---|---|
| `ef6061a5` feat: project typeahead in ⌘K command palette (#8773) | The ⌘K command palette now promotes fuzzy-matched **Projects** alongside Tasks/Actions. | The command palette is **not documented anywhere** in our docs (no `⌘K` / "command palette" reference exists). Documenting this enhancement means first authoring a palette/navigation page from scratch — net-new scope beyond a sync edit. Decide whether to create a new "getting around / keyboard navigation" page or fold it into an existing user guide, then it can be drafted. |

## Context-only / internal (no user- or developer-facing doc surface)

These landed in the window but changed only internal behaviour, debug metadata, or cosmetics — no documented surface to update:

| Parent commit | Note |
|---|---|
| `a7a73d5b` Fix stale server info debug metadata (#8753) | Debug-only metadata on server info; not a documented field. |
| `4a2447da` [codex] Fix markdown contrast on accent bubbles (#8689) | Cosmetic CSS/contrast fix in issue chat bubbles (`index.css`, `IssueChatThread`, `MarkdownAccentStyles`). See Screenshots advisory below. |
| `f019f54b` Fix active heartbeat run reaping (#8776) | Internal reliability fix in the heartbeat service. |
| `openapi.ts` `localChanges` schema | A `localChanges` git-status object was added to a nested workspace-commit info schema in the OpenAPI spec. Deeply nested field on an existing response; our hand-written API reference summarises rather than enumerates it — no edit warranted. |
| X mention poller (`#8707` + revert `#8709`) | Feature and its revert both fall inside the cumulative window and net to zero — never reached the manifest. |

## Quarantined (< 24h old at this run — eligible next run)

Cutoff this run: `2026-06-30T08:48:11Z`. These commits are held back so any revert has time to land. **The next nightly run will process them**, most notably the reusable-sandbox-custom-images feature:

| Parent commit | Surface |
|---|---|
| `8d9f9fd9` Add reusable sandbox custom images (#8794) | **New feature** — reusable sandbox custom images (new DB schema, shared validators, server routes/services, UI). Likely warrants doc coverage next run. |
| `a8f0ebaa` Refresh run config before reusing workspaces (#8797) | Workspace run-config refresh behaviour. |
| `3e31bf09` [codex] Add pipeline automation title templates (#8787) | Pipeline automation title templates. |
| `b4fccaa8` [codex] Unify issue media attachment gallery (#8785) | Issue media attachment gallery (UI). |
| `37c097a4` [codex] Update watchdog properties pane cache (#8786) | Watchdog properties pane cache (UI). |
| `0c2ec7de` Fix sandboxed Claude and Codex probe behavior (#8775) | Adapter probe behaviour under sandboxing. |
| `b61c2852` build(ui): drop redundant tsc type-check from ui build (#8781) | Build-only. |
| deps bumps (`#8745 #8742 #8737 #8736 #8739 #8740 #8743 #8744 #8738 #8741`) | Dependency version bumps. |

## ⚠ Drift

None. `check-drift.mjs --against master` reported 0 drift records — every documented parent path, CLI command, env var, and REST route still resolves upstream.

## ⚠ Reconcile

None. No previously-applied doc edit corresponds to a surface that has since disappeared upstream. (Last run outcome was `no-changes`; nothing prior to reconcile.)

## Screenshots

No registry entry `depends_on` any UI source file changed in this window, so nothing is flagged for recapture. **Advisory:** the accent-bubble contrast fix (`#8689`) touches global `index.css` and issue chat rendering; the registry does not track `index.css` as a dependency (by design — it would mark everything stale). If a chat-thread screenshot looks off, recapture manually with `npm run screenshots:refresh`. See `SCREENSHOTS_PENDING.md`.

## ⚠ Pre-existing build/lint warnings (not introduced by this run)

- **`sync:lint-links` reports 1 "broken internal link"** in `docs/reference/skills/optional/research/last30days.md` (`-> url`) — a false positive: the checker parses ``[name](url)`` inside an inline-code span (prose documenting markdown syntax) as a real link. Fix belongs in `scripts/sync/lint-links.mjs` (skip inline-code spans) or the example doc — outside this sync's scope.
