# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the current cumulative view from the last parent release; it retains already-applied work for context and only treats the newly-settled delta as authoring scope.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `90f85a7` (24-hour cutoff `2026-07-14T08:09:50Z`). The compare helper saw **104 commits / 934 changed files** with **0 truncated leaves**. Newly settled since the prior run: `c36f1a4…90f85a7` (**9 commits / 62 files**) — exactly the set held back as quarantined last run.

## ✅ Applied in earlier nightly drafts

- **Cases API** — `docs/reference/api/cases.md`.
- **Built-in Agents API** — `docs/reference/api/built-in-agents.md`.
- **Human permission keys** — `docs/administration/roles-and-permissions.md`.
- **Codex `gpt-5.4-mini`** — `docs/reference/adapters/codex.md`.
- **Decisions, ACP, inbox, and interaction contracts** — API, guide, adapter, and CLI pages updated in PR #57.
- **Redundant issue re-wake throttling** (`5dff526`) — `docs/how-to/debug-stuck-heartbeat.md`.

## ✍️ Newly settled, applied

- **`costStatus` on cost events** (`efcce9c`) — `docs/reference/api/costs.md` now documents the new `costStatus` field on `POST /api/companies/{companyId}/cost-events`. It records whether token usage is priced (`reported`) or was captured without a resolvable dollar cost (`unpriced`), defaults to `reported` when omitted, and a short callout explains why a local-CLI run with unknown pricing is marked `unpriced` instead of logged as if it cost nothing. Verified verbatim against `createCostEventSchema` and `COST_STATUSES` at the quarantine endpoint.

## ↻ Newly settled, context-only

- **`branchName` on the server git snapshot** (`6f20460`) adds `branchName: string | null` to `ServerGitInfo` (and its OpenAPI registration). We do **not** publish a server-info / git-snapshot API page, so there is no doc surface to update — surfaced for awareness only.
- **PID persistence via `onSpawn`** (`0e21a27`) forwards an `onSpawn` callback through the Hermes and process adapters and `runChildProcess`. It is internal runtime plumbing with no documented adapter config field or contract change.
- **Per-run usage basis / null cost passthrough** (`efcce9c` support) — the Claude- and Codex-local adapters now always mark parsed usage `per_run` and let `costUsd` be `null` when the stream reports no price. The user-visible effect surfaces through the `costStatus` field above; no other doc change.
- **UI restyle wave** — status-colour/inbox-badge consolidation (`634ae12`), the experiments auto-recovery dialog fix (`b49d178`), and the unreleased-build source-SHA UI (`6f20460`) touch tracked UI files but add no new workflow or control. Recapture candidates are flagged in `SCREENSHOTS_PENDING.md`, not authored.
- **Telemetry proposal extractor** (`90f85a7`) and the **stable-release dry-run notes gate** (`6a36ae4`) are parent-repo contributor/release tooling under `scripts/**`, not product surfaces.
- **CI shard balancing** (`ce7dedf`) and the **runtime asset build-gap guard** (`8775bde`) are parent CI/build internals.

## ⚠ Drift — surfaced, not applied

No candidates. The canonical-parent drift check against `master` at `7947308` completed cleanly (8 parent paths, 57 CLI commands, 65 env vars, 270 REST routes, 11 permission keys, 4 roles).

## ↻ Renames / removals — surfaced, not applied

- **`packages/adapters/acpx-local` removed with no rename target** (ongoing, cumulative window). Its existing page remains the retirement/migration reference; a human should decide whether it should become redirect-only. No new page was created. The newly-settled delta itself contains no directory renames, additions, or removals.

## 🕒 Quarantined for the next run

Held back by the 24-hour cutoff (`90f85a7…master`, **31 commits**). Feature-bearing items to watch next run:

- **MCP gateway, split 1–8/8** (`7b35de6`, `1de0a3b`, `cfa5e07`, `931eec3`, `9c8adee`, `a116713`, `f712ced`, `3db2e6b`; #9556–#9563) — governed access contracts, tool access policy, gateway runtime, adapter/deployment integration, Tools/Profiles/Apps UI, e2e coverage and operator docs. Likely a substantial new documentation surface.
- **`feat(adapters): confine local coding processes`** (`98d9360`, #9504) — adapter process sandboxing.
- **`feat(run-logs): durable run-log store via object-storage mirror`** (`b4e7ba5`, #8984) — may add config/env surface.
- **`security(invites): widen invite-token entropy and rate-limit public invite endpoints`** (`1cfed0c`, #8979) — invites API behaviour.
- **`security(server): close cross-tenant existence oracle (404 instead of 403)`** (`7f2ed0a`, #3967) — API status-code contract change.
- **`fix(worktree): require target attestation before repair`** (`c4eb533`, #9414).
- Plus dependency bumps, `fix(codex)` auth-failure classification, a decision-card supersede fix, a ripgrep base-image build, and a telemetry-marker workflow doc.

## 📸 Screenshots

- **246 of 298** registry screenshots across **61 routes** are recapture candidates in the cumulative window. This run, **9 of those routes** intersect the newly-settled delta (Inbox, Issue detail, chat thread, company-menu, experimental + general instance settings) via `634ae12` / `b49d178` / `6f20460`. Flagged in `SCREENSHOTS_PENDING.md`; capture belongs in a separate human-review PR and is never auto-pushed.

## Verification

The authored `costStatus` guidance was checked by hand against canonical parent `packages/shared/src/validators/cost.ts`, `packages/shared/src/constants.ts`, and `server/src/routes/costs.ts` at the quarantine endpoint `90f85a7d11c517b1d09db90dbec97f4de7d96b83`. The field name `costStatus`, the enum values `reported` / `unpriced`, the `reported` default, and the fact that `POST /api/companies/{companyId}/cost-events` validates the body with `createCostEventSchema` all match the source verbatim. `scripts/sync/verify-edit.mjs` reported 19/19 claims verified, 0 unverified, 0 suspicious.

_Manifest hash: `14e79a2f0035a6d41de532224d34b317a98742e23f151bf405a3c56ea36dad88`_
