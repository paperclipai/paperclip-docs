# PENDING — nightly docs sync

_Regenerated by `/sync-docs` (nightly mode) on 2026-08-12. Overwritten each run from the current change manifest — do not hand-edit._

- **Window:** parent `v2026.722.0` (base release) → quarantine boundary `9cdaa54` (newest commit older than 24h). Parent HEAD is `67001ec`; the 28 newest commits (`b58ce27`…`67001ec`, all dated 2026-08-11T12:56Z or later) are held back by the 24h quarantine (cutoff `2026-08-11T11:21Z`).
- **New since last run** (`6a4e2e1` → `9cdaa54`): 14 commits, 54 files.
- **Drift (Phase 1.5):** none — 0 records across all scanned parent paths, CLI commands, env vars, REST routes, and permission keys.
- **Reconcile (Phase 3.5):** none — the newly-settled commits are pure additions/refinements; no previously-documented surface disappeared from the cumulative diff.

## Applied this run

**Company-import zip upload cap is now operator-configurable** (paperclip `5ca752d`, #11184).

- The server default upload cap for a company-import `.zip` rose to **1 GB**, and operators can override it with a new `PAPERCLIP_IMPORT_ZIP_MAX_BYTES` environment variable (bytes; clamped to 1 byte – 64 GiB; invalid values fall back to 1 GB). Source: `server/src/http/body-limits.ts`.
- Edits (auto-merge tier, committed to `nightly`):
  - `docs/reference/deploy/environment-variables.md` — new row under **Storage**.
  - `docs/guides/power/export-import.md` — factual note in "Importing a large company" pointing operators at the override.

## Triaged, no doc action this run

- **Codex adapter secure device-login building blocks** (paperclip `4594696`, #11097 — `packages/adapters/codex-local/src/server/device-login-{export,parse,runner}.ts`). Half-built plumbing; the user-facing auth flow is not wired up yet, and more codex-auth work (e.g. `CODEX-AUTH-CACHE.md`) sits in the quarantine window. Defer until the flow lands and stabilises.
- **Release channels / lane-separated Docker tags** (paperclip `f917378`, `8f7b8b3` — smoke-gated nightly channel, human-gated beta channel). Internal CI/release plumbing. The user-facing `paperclipai channels` CLI command (#11210) is currently quarantined; document channels once that command settles.
- **Import correctness refinements** (`35aaaa0` preserve task timestamps/hierarchy, `d816ec8` keep imported tasks quiescent under productivity review, `4c062a0` stop coercing imported agents to the CEO adapter). Internal behavioural fixes; export-import guide already describes fidelity at the level users need.
- **Inbox UX** (`9cdaa54` remember folded subtasks, archive undo toast). Minor UI polish; surfaced as screenshot staleness, not prose.
- **`db-migrations` (context-only):** no direct prose.

## Quarantined — expected next run

The 28 commits held by the 24h quarantine include several user-facing surfaces to pick up next cycle:

- `feat(cli): add 'paperclipai channels' to show release lanes and the current one` (#11210).
- `feat(server): chunked import preview endpoint and resumable upload in the Import page and CLI` (#11224) and `chunked resumable company import transfers` (#11223).
- `Let tenants edit env vars on managed sandbox environments; add managed-sandbox-only mode` (#11200).
- `feat: make chat-style tasks the default experience` (#11101).
- `Remove decision training UI` (#11225) — watch for a doc-removal / reconcile flag next run.
- `feat(ui): surface all issue documents and agent artifacts in chat-style sidebar` (#11226).

## Screenshots

See `SCREENSHOTS_PENDING.md` for staleness. Recapture with `npm run screenshots:refresh`; PNGs land in a PR for human review, never auto-pushed.
