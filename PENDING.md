# PENDING — nightly docs sync

Regenerated by `/sync-docs` (**nightly mode**, staged on `nightly` for review — not merged, not pushed). This file is overwritten each run — it always reflects the current cumulative manifest, not an append log.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `05973b2` (24h quarantine cutoff `2026-07-10T08:25Z` held back 30 newer commits, up to `e4e12bf`). **Newly-settled delta this run: `3e63a7e…05973b2` (21 commits) — the features the last run deferred into quarantine have now aged past 24h.**

Last run: 2026-07-11 · outcome: applied (staged on `nightly-draft`)

## ✅ Applied this run

The last run's quarantine cutoff was `3e63a7e` (2026-07-09). A day later, the built-in-agents, Cases, and permission changes it explicitly held back have settled, so this run picks them up as a group.

**1. Human permission keys — `agents:configure`, `agents:suggest-changes`, `skills:suggest-changes`** (parent `#9206`, `packages/shared/src/constants.ts` + `server/src/services/company-member-roles.ts` + `authorization.ts`)

- `docs/administration/roles-and-permissions.md` — grew the key count from **ten → thirteen**. Added `agents:configure` to the **Owner** and **Admin** implicit-grant bundles (verified against `grantsForHumanRole()` — both roles now carry it; the Owner-vs-Admin difference is still only `users:manage_permissions`). Added the two `*:suggest-changes` keys as **explicit-only** and documented the new **direct-vs-suggest pairs**: `agents:configure`/`agents:suggest-changes` and `skills:create`/`skills:suggest-changes` (propose-for-review vs apply-directly, the mechanism behind Reflection Coach). *This is the drift item deferred last run — it was inside the 24h quarantine at that time; now settled and applied.*

**2. Cases — experimental first-class case object** (parent `#9198`, `server/src/routes/cases.ts` + `packages/db/src/schema/cases.ts`)

- **NEW page `docs/reference/api/cases.md`** (nav: API section, after *Issues*). Documents the full real endpoint set (create/list/get/patch cases, issue links in both directions, the case-document surface with revisions/lock/restore/annotations, attachments, and the event timeline), the six statuses, three link roles, and the identity model. Leads with a prominent **experimental / off-by-default** callout — every route calls `assertCasesEnabled()` and returns `403 "Cases are disabled"` unless the instance experimental setting `enableCases` is on.

**3. Built-in agents** (parent `#9206`, `server/src/routes/built-in-agents.ts` + `services/built-in-agents.ts`)

- **NEW page `docs/reference/api/built-in-agents.md`** (nav: API section, after *Agents*). Documents what a built-in agent is (registry-`key`-resolved agent carrying the immutable `metadata.paperclipBuiltInAgent` marker), the **three shipped built-ins** (`briefs`, `learning`, `reflection-coach` — the parent's own doc still says "the first built-ins are briefs and learning", which is stale; the doc reflects the real `DEFINITIONS`), the lifecycle states (`not_provisioned`, `pending_approval`, `needs_setup`, `ready`, `paused`), the eight real routes (list/status/reconcile/provision/reset + routine enable/disable/run), and the `412 built_in_agent_not_configured` / `built_in_agent_paused` contract. Notes the `enableBuiltInAgents` instance flag. Cross-links Reflection Coach to the two new permission keys above.

**4. Codex adapter — `gpt-5.4-mini`** (parent `#9352`, `packages/adapters/codex-local/src/index.ts`)

- `docs/reference/adapters/codex.md` — added `gpt-5.4-mini` to the known Codex model-id list.

Build (`docs:build`, 160 files) and `sync:check` (lint-links + verify-nav) both pass. Nav orphans remain the two intentional CLI redirect anchors (`commands.md`, `control-plane-commands.md`).

### Phase 5.5 verification
The automated `verify-edit.mjs` pass again hung on its recursive parent-tree fetch (the known `api.github.com` instability from the prior run). Verification was done **by hand** against parent source read directly at ref `05973b2`: every endpoint path, lifecycle state, case status, link role, error string (`Cases are disabled`, `built_in_agent_not_configured`, `built_in_agent_paused`), permission key, and the `gpt-5.4-mini` id matched verbatim. No unverified high-confidence claims.

## ↻ Context-only (no doc edit)

- **Grok local → remote-managed environments** (`#9338`) — `grok_local` was added to `REMOTE_MANAGED_ADAPTERS` in `packages/shared/src/environment-support.ts`, so Grok agents can now run in sandbox/remote execution targets. **No `grok-local.md` edit made**: the sibling `-local` adapter docs (codex, gemini, claude, opencode) do not document per-adapter environment support, so adding it only to Grok would be inconsistent. Flagged for a possible cross-adapter "supported environments" note in a future pass.
- **OpenCode models** (`openai/gpt-5.5`, `openai/gpt-5.4-mini`) — added upstream to the `opencode_local` default-selection list. `opencode.md` documents `provider/model` format and points to `opencode models` rather than enumerating a fixed list, so no doc edit needed.

## ⚠ Drift (Phase 1.5) — surfaced, not applied

- **7 REST routes (MEDIUM → likely false positives).** `teams-catalog.md` (6 routes) and `dashboard.md` (1 route) flagged by the scanner, but `server/src/routes/teams-catalog.ts` and `dashboard.ts` still exist upstream — the scanner misses them via route-prefix/dynamic registration. **Verify:** spot-check before acting. Same finding as last run; no action.

## 🕒 Quarantined — deferred to next run (30 commits < 24h old, `05973b2…e4e12bf`)

Notable feature-level changes past the quarantine cutoff (`2026-07-10T08:25Z`), not yet processed:

- **feat: add attention queue and Decisions surface** (`#9380`) — new `server/src/routes/attention.ts` + `services/attention.ts`. A new operator surface; will need a doc once settled.
- **feat: run ACP sessions in sandbox execution targets** (`#9390`).
- **Search: filters, sorting, operators & command-palette parity** (`#9327`) — large `ui/src` change; screenshot + user-guide impact.
- **Skill Studio: three-pane skill IDE with sandboxed test runs** (`#9241`) — new UI surface; screenshot + guide impact.
- **feat(ui): Lucide icons for task status glyphs** (`#9395`) — screenshot impact.

## 📸 Screenshots

- The large `ui/src` restyle wave plus this window's new UI features (Search `#9327`, Skill Studio `#9241`, task-status icons `#9395`) leave most registry screenshots stale — see `SCREENSHOTS_PENDING.md`. Flagged, not recaptured (capture lands in a separate PR via `npm run screenshots:refresh`).
- **1 uncaptured target (from the main-merge, not this sync):** `interactions/checkbox-confirmation-card`, referenced in `docs/reference/api/checkbox-confirmations.md`, has no capture target in `scripts/screenshots/routes.mjs` — it will go stale silently. Worth adding a target.

_Manifest hash: `1552f1197d2113b27bcc495414c14fc055984ff168ada5edee2a209ac51f8636`_
