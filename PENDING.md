# PENDING — nightly docs sync

Regenerated by `/sync-docs` (nightly mode). Cumulative window: parent release `v2026.626.0` → `master` HEAD `e6407b32`.

This file is overwritten each run — it always reflects the current run, not an append log.

## Applied this run (committed to `nightly`)

**Nothing authored.** The cumulative window since the new base release `v2026.626.0` contains only two parent commits, and they cancel out:

| Parent commit | Surface | Outcome |
|---|---|---|
| `44e2ab53` feat: add X mention poller backend (#8707) | X mention poller backend | Reverted by `#8709` below — net-zero in the cumulative diff |
| `e6407b32` refactor: revert X mention poller backend (#8709) | Revert of the above | — |

The cumulative-diff design auto-cancels the feature and its revert before they ever reach the manifest, so there is nothing to document. Both commits are also younger than the 24h quarantine cutoff (`2026-06-28T10:08:19Z`), so they would have been deferred regardless.

> **State reconciled this run.** `.sync-state.json` was stale: the **v2026.626.0** release docs were produced and merged to `main` (PR #36, 2026-06-27) but the state file's `base_release_tag` was never bumped off `v2026.618.0`. This run resets the base to `v2026.626.0` (parent `4c6c0c6`) and switches `branch_mode` back to `nightly`, so future cumulative windows start from the correct, already-shipped baseline.

## Quarantined (eligible next run, < 24h old at this run)

Cutoff this run: `2026-06-28T10:08:19Z` (now − 24h). No quarantined doc-worthy commits — the only two in-window commits are the self-cancelling X-mention-poller pair above.

## ⚠ Drift

None. `check-drift.mjs --against master` reported 0 drift records — every documented parent path, CLI command, env var, and REST route still resolves upstream.

## ⚠ Reconcile

None. The cumulative window is empty, so no previously-applied surface disappeared upstream.

## Screenshots

No screenshots are pending recapture — the cumulative window touches no UI source files that any registry entry `depends_on`. See `SCREENSHOTS_PENDING.md`.

## ⚠ Pre-existing build/lint warnings (not introduced by this run)

- **`sync:lint-links` reports 1 "broken internal link"** in `docs/reference/skills/optional/research/last30days.md` (`-> url`) — a false positive: the checker parses ``[name](url)`` inside an inline-code span (prose documenting markdown syntax) as a real link. Fix belongs in `scripts/sync/lint-links.mjs` (skip inline-code spans) or the example doc — outside this sync's scope.
