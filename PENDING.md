# PENDING — nightly docs sync

Regenerated by `/sync-docs` (**nightly mode**, staged on `nightly` for review — not merged, not pushed). This file is overwritten each run — it always reflects the current cumulative manifest, not an append log.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `3e63a7e` (24h quarantine held back 21 newer commits). **38 commits, 533 changed parent files, `truncated_leaves=0`.**

Last run: 2026-07-10 · outcome: applied (staged on nightly-draft)

## ✅ Applied this run

**ACP engine graduation + `acpx_local` removal** — the standalone `acpx_local` adapter was deleted upstream and its Agent Client Protocol capability folded into the mainstream local adapters. Driving parent commit: `eedc7ddef` "Make ACP the default engine for local adapters (#9238)" (settled, pre-quarantine).

- `docs/reference/adapters/claude-code.md`, `codex.md`, `gemini-cli.md` — added an **ACP Engine** section + an `engine` row in Common Fields. Documents the `engine` field (`auto` = ACP-preferred default / `acp` / `cli`) and the ACP options `agentCommand`, `mode` (default `persistent`), `nonInteractivePermissions` (default `deny`), `stateDir`, `warmHandleIdleMs` (default `0`). ACP server-command binaries per adapter: `claude-agent-acp`, `codex-acp`, `gemini --acp`. Every identifier copied verbatim from parent `packages/adapters/<name>/src/server/config-schema.ts` and `packages/adapter-utils/src/acpx-engine/constants.ts`.
- `docs/reference/adapters/overview.md` — removed the four `acpx_local` references (Choose-an-Adapter row, Built-In Adapters row, Next Steps link) and rewrote the **Feedback Granularity** section: the richest structured stream is now the ACP engine on the local adapters, not a standalone adapter.
- `docs/reference/adapters/sandbox-providers.md` — removed `acpx_local` from the `adapterType` enum.

Build (`docs:build`) and `sync:check` (lint-links + verify-nav) both pass. The 2 nav orphans (`commands.md`, `control-plane-commands.md`) are the intentional CLI redirect anchors.

> **Note on Phase 5.5:** the automated `verify-edit.mjs` pass could not complete this run — its recursive parent-tree fetch hung on network instability (connection resets to api.github.com). All authored identifiers were instead verified by hand against the parent source files read directly this run.

## ⚠ Reconcile — `acpx-local.md`

`docs/reference/adapters/acpx-local.md` still exists and still documents the now-removed adapter. Rename detection classified `packages/adapters/acpx-local` as `removed_dirs_no_match` (no rename target). Per policy the page was **not** deleted — a decision (deprecate-in-place stub vs delete vs leave) is tracked in **[paperclipai/paperclip-docs#55](https://github.com/paperclipai/paperclip-docs/issues/55)**. It is no longer linked from `overview.md`, but remains in `content.json` nav.

## ⚠ Drift (Phase 1.5) — surfaced, not applied

- **`agents:configure` permission (HIGH confidence, verified real, but DEFERRED).** Parent now has 13 `PERMISSION_KEYS` vs the 10 documented in `docs/administration/roles-and-permissions.md` — three new keys (`agents:configure`, `agents:suggest-changes`, `skills:suggest-changes`), and `agents:configure` is granted to `owner` + `admin` in `grantsForHumanRole()`. **However**, all of this was introduced by commit `8b6a06ee2` "Add built-in agents and Reflection Coach bundle (#9206)" at 2026-07-09T21:29 — **inside the 24h quarantine**. At the settled window endpoint `3e63a7e` the keys do not yet exist. It is bundled with the (also-quarantined) built-in-agents feature. **Held for the next run**, when #9206 clears quarantine, so the whole feature (docs + permissions) syncs together and any revert has time to land. *(You asked to include this fix; the double-verify is exactly what flagged it as unsettled — say the word if you want it drafted now anyway.)*
- **7 REST routes (MEDIUM → false positives).** `dashboard.md` and `teams-catalog.md` routes flagged by the scanner, but the route files (`server/src/routes/dashboard.ts`, `teams-catalog.ts`) still exist upstream — the scanner missed them via route-prefix registration. No action.

## 🕒 Quarantined — deferred to next run (21 commits <24h old)

Notable feature-level changes past the quarantine cutoff, not yet processed:

- **Built-in agents + Reflection Coach bundle** (#9206) — new `docs/built-in-agents.md` upstream; carries the `agents:configure` permission above.
- **Cases: experimental first-class case object** (#9198).
- **Search: filters, sorting, operators & command-palette parity** (#9327).
- **Enable sandbox environments for Grok local adapter** (#9338).

## 📸 Screenshots

- **244 of 296** registry screenshots are stale for this window across **61 routes** — see `SCREENSHOTS_PENDING.md`. This is dominated by the large `ui/src` restyle wave; most are cosmetic. Flagged, not recaptured — capture lands in a separate PR (`npm run screenshots:refresh`).
