# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the cumulative view from the last parent release, with already-applied items retained for context and only the newly-settled delta authored in this run.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `e4e12bfb` (24-hour cutoff `2026-07-11T09:49:05Z`). The compare helper saw **89 commits / 900 changed files** with **0 truncated leaves**. The newly-settled delta is `05973b2…e4e12bf` (**30 commits**).

## ✅ Applied in earlier nightly drafts

- **Cases API** — `docs/reference/api/cases.md`.
- **Built-in Agents API** — `docs/reference/api/built-in-agents.md`.
- **Human permission keys** — `docs/administration/roles-and-permissions.md`.
- **Codex `gpt-5.4-mini`** — `docs/reference/adapters/codex.md`.

## ✅ Applied in this draft

- **Decisions attention queue** — added `docs/reference/api/attention.md` and the end-user/operator guide `docs/guides/day-to-day/decisions.md`. The new board-only feed is `GET /api/companies/{companyId}/attention`; the guide covers its `/decisions` workflow, filtering, inline decisions, dismissal, and snoozing.
- **ACP in sandbox environments** — updated Claude Code, Codex, Gemini CLI, and adapter-overview reference pages. `engine: "auto"` keeps ACP only when a sandbox supplies a bidirectional process session; one-shot sandboxes and non-sandbox remote targets fall back to CLI. The pages now also describe the 14,400-second sandbox default for `timeoutSec`.
- **Codex models** — updated the known model list and default to `gpt-5.6`, with `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna`.
- **Inbox snoozing** — clarified that `paperclipai inbox dismiss` also accepts `kind: "snooze"` and a future `snoozedUntil` timestamp for `approval:`, `join:`, `run:`, and `attention:` items.
- **Built-in-agent switch** — documented the experimental `enableBuiltInAgents` boolean in the settings and Instance Admin API references.
- **Issue interactions** — corrected the five current kinds and the `none`, `wake_assignee`, and `wake_assignee_on_accept` continuation policies.

## ↻ Context-only / manual review

- **Built-in-agent asset and startup fixes** — the existing Built-in Agents page already matches the canonical registry, routes, keys, states, and `enableBuiltInAgents` gate; no duplicate rewrite was made.
- **`acpx_local` removal** — the cumulative rename pass reports `packages/adapters/acpx-local` removed with no rename target. Its page remains intentionally published as the retirement/migration reference; a human can decide later whether a redirect-only treatment is preferable.
- **Drift check** — no candidates at parent `master` (`4a40c0c`). The earlier route false-positives did not recur.

## 🕒 Quarantined for the next run

- **`4a40c0c` — scheduled runs gated on external activity**. It is still inside the 24-hour window and has not been documented or otherwise applied.

## 📸 Screenshots

- **246 of 298** registry screenshots across **61 routes** are stale candidates in the cumulative window. They are flagged in `SCREENSHOTS_PENDING.md`, not recaptured. Capture output belongs in a separate human-review PR.

## Verification

Every authored identifier was checked by hand against canonical parent source at `e4e12bfb890a0fdf4c7de092362472c50a584533`: routes, query flags, errors, `sourceKind`/severity values, dismissal fields, interaction kinds and policies, `enableBuiltInAgents`, ACP engine values, `timeoutSec` behavior, and Codex model ids. No high-confidence unverified claim remains.

_Manifest hash: `492a91956e32d8c6d6cd3b01f9d1a2ad549183c26a7cade3d0be9f252765608e`_
