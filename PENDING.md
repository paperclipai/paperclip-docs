# Pending doc sync — nightly

_Regenerated by `/sync-docs` (nightly mode). Cumulative base `v2026.722.0` (`e55d702`) → parent `master` HEAD `f0e6c0f`. This file reflects the current run's manifest; it is overwritten each run._

Run date: 2026-08-13 · Drift: clean · Reconciliation: clean

## Applied this run (PR-tier drafts on `nightly`)

New parent surfaces from the incremental window `67001ec…f0e6c0f` (40 commits):

- **Adapter setup-token / device-login authentication** — new page `docs/reference/adapters/setup-token-authentication.md` + cross-refs in `claude-code.md`, `codex.md`, `overview.md`. Grounded in `setup-token-parse.ts`, `setup-token-runner.ts`, `setup-token-session.ts`, `adapter_auth_sessions.ts`, codex `device-login-*.ts`, `codex-device-login-service.ts`.
- **Company onboarding seed** — new "Onboarding seed" section in `docs/reference/api/companies.md`. Route `POST /api/companies/{companyId}/onboarding-seed` (`routes/onboarding-seed.ts`, `services/onboarding-seed.ts`, `company_onboarding_seeds` schema, migration 0216).
- **Routines webhook replay guard** — updated `docs/reference/api/routines.md` Fire Public Trigger section for the new HMAC replay rejection (409) on timestamped `hmac_sha256` signing (parent commit 676e20a).
- **opencode model probe** — updated `docs/reference/adapters/opencode.md`: the `opencode models` availability probe is now best-effort/non-fatal.

## ⚠ Drift

None. (`sync:check-drift` against master returned 0 records. The `sync:check` npm default flagged the new onboarding-seed route against the stale ref `67001ec` — confirmed false positive; the route exists on master.)

## ⚠ Reconcile

None. No previously-applied entries disappeared from the cumulative diff.

## Screenshots

See `SCREENSHOTS_PENDING.md` — 86 of 310 tracked screenshots are stale (driven by `AgentConfigForm.tsx`, `AgentDetail.tsx`, `IssueDetail.tsx`, `ApprovalPayload.tsx`, `NewAgent.tsx`, `CompanyEnvironments.tsx`, `NewIssueDialog.tsx`). Flagged for human review; not auto-captured.

## Notes / not documented (internal or already-covered)

- Large web-ui churn (document-annotation popover refactor, AgentConfigForm/agents-route growth, task-chat components) — refinements to already-documented surfaces or internal; no new user-facing surface isolated this run.
- `tool-access.ts` / `access.ts` route edits — incremental changes to already-documented tool-gateway/access surfaces.
