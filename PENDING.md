# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the current cumulative view from the last parent release; it retains already-applied work for context and only treats the newly-settled delta as authoring scope.

Cumulative window: parent release `v2026.707.0` (`390627b`) → parent `master` quarantine endpoint `4a40c0c` (24-hour cutoff `2026-07-12T08:01:38Z`). The compare helper saw **90 commits / 902 changed files** with **0 truncated leaves**. Newly settled since the prior run: `e4e12bf…4a40c0c` (**1 commit / 5 files**).

## ✅ Applied in earlier nightly drafts

- **Cases API** — `docs/reference/api/cases.md`.
- **Built-in Agents API** — `docs/reference/api/built-in-agents.md`.
- **Human permission keys** — `docs/administration/roles-and-permissions.md`.
- **Codex `gpt-5.4-mini`** — `docs/reference/adapters/codex.md`.
- **Decisions, ACP, inbox, and interaction contracts** — the API, guide, adapter, and CLI pages updated in PR #57.

## ↻ Newly settled, context-only

- **Scheduled routine activity gate** (`4a40c0c`) adds the persistence and scheduler foundation for activity-aware scheduled runs. The real schema defaults are `activityGatePolicy: "always"` and `activityGateScope: "company"`, preserving existing behavior. The parent change explicitly defers public API validation and documentation, so no page was authored from internal fields or inferred behaviour. Revisit when the public configuration contract lands.
- **`acpx_local` removal** — rename detection reports `packages/adapters/acpx-local` removed with no target. Its existing page remains the retirement/migration reference; a human should decide whether it should become redirect-only.

## ⚠ Drift — surfaced, not applied

No candidates. The apparent 66-candidate result from the sandboxed check was a GitHub connectivity false-positive; the required elevated check against canonical parent `master` at `c36f1a4` completed cleanly.

## 🕒 Quarantined for the next run

- `c36f1a4` — mobile Decisions rows.
- `8a0db22` — Decisions scrolling performance.
- `c8253e3` — fresh-heartbeat execution-contract injection.
- `5dff526` — redundant issue re-wake throttling.
- `9e7e84e` — ACP-lane spend telemetry.

## 📸 Screenshots

- **246 of 298** registry screenshots across **61 routes** remain stale candidates in the cumulative window. They are flagged in `SCREENSHOTS_PENDING.md`, not recaptured; capture output belongs in a separate human-review PR.

## Verification

No user-facing page was authored in this run. The context-only decision was checked by hand against canonical parent `packages/db/src/schema/routines.ts` and `server/src/services/routines.ts` at `4a40c0cb13fd0e8cd1213b71599c4d7275b51582`: the fields and their `"always"` / `"company"` defaults match verbatim, and no public route or validation contract exists there.

_Manifest hash: `1629c22a08c0604f79c217982c702ec458122e0749b30354b4ca9c6eab5c5a59`_
