# PENDING — release-mode docs sync

Regenerated by `/sync-docs` (**release mode**, staged on `nightly` for review — not merged, not pushed). Cumulative window: parent release `v2026.626.0` (`4c6c0c6`) → `v2026.707.0` (`390627b`). 93 commits, 577 changed parent files, `truncated_leaves=0`.

This file is overwritten each run — it always reflects the current run, not an append log.

Last run: 2026-07-08 · outcome: applied (staged on nightly)

## ✅ Applied this run

- **The Work Timeline guide** · `docs/guides/day-to-day/work-timeline.md` (merged from PR #48)
  - Company-scoped Gantt-style timeline of when agents worked, handoffs, and overlap; boundary vs the Activity Log. Backed by parent `server/src/services/work-timeline.ts` + `packages/shared/src/types/work-timeline.ts` (release Highlight, #8938/#8880/#8875/#8923).

- **Secret scopes and the responsible user** · `docs/administration/secret-scopes.md` (merged from PR #47)
  - Company vs user secret scope, responsible-user run attribution, and the pre-dispatch secret check. Backed by parent `user_secret_declarations`/`user_secret_definitions` schemas + `responsible-user-denial` (release Highlight, #8825).

- **Custom sandbox images** · `docs/guides/projects-workflow/custom-sandbox-images.md` (new)
  - Operator-facing guide for the "custom sandbox images with a built-in SSH terminal" Highlight (#8911/#8794/#8850): turning on the experimental **Enable Environments** gate, the build → browser-terminal prep → capture flow, runtime use of the active image, and Refresh/Rollback/Disable management. **Fulfils the operator-guide item deferred by the 2026-07-04 nightly run** (the plugin-SDK developer surface was already documented then). Every concrete claim traced to parent `server/src/services/environment-custom-images.ts`, `environment-custom-image-runtime.ts`, `realtime/environment-custom-image-terminal-ws.ts`, `ui/src/pages/CompanyEnvironments.tsx`, `packages/shared/src/constants.ts`, migrations `0125`/`0127`. Stamped `paperclip_version: v2026.707.0`.

- **Ramp optional skill** · `docs/reference/skills/optional/finance/ramp.md` (new) + `optional.md` + `content.json`
  - New optional catalog skill (release Improvement, #9157): wraps Ramp's official agent playbooks behind Paperclip's approval gates and host allowlist. New **Finance** category in the optional-skills nav. Metadata verified against parent `packages/skills-catalog/generated/catalog.json`.

## 🕒 Backlog — release Improvements not given dedicated pages

These v2026.707.0 changes are refinements to existing (or UI-only) surfaces; noted here rather than silently dropped:

- **Redesigned environment variables editor** (#8930) — single reusable editor replacing the legacy `EnvVarEditor`. UI refinement of existing env-var config; no new page, but drives screenshot staleness (see below).
- **Pipeline automation** (#8787 title templates, #9090 health-warning dedup) — refinements to a **pre-existing, still-undocumented** pipelines feature. A first-class pipelines guide remains unwritten — carry forward as a standalone authoring task, not a v2026.707.0 delta.
- **Starred sidebar resources** (#9085), **one-click recovery for diverged work** (#9136), **live heartbeat progress in run logs** (#8965), **live descendant status in inbox rows** (#8876) — UI polish; candidates for brief mentions in existing guides on a future pass.
- **Gemini 3.1 Pro models** on `gemini-local` (#1602) — adapter model list; fold into `docs/reference/adapters/gemini-cli.md` next pass.

## 📸 Screenshots

- **134 of 284** registry screenshots are stale for this window (36 routes) — see `SCREENSHOTS_PENDING.md`. Not recaptured; capture lands in a separate PR for review (`npm run screenshots:refresh`).
- The four new pages this run ship without captures (custom-sandbox-images has no screenshots yet — highest-value shot: the setup session with the browser terminal open).

## ⚠ Drift

- None. `check-drift.mjs --against master` reported **0 drift records** — every documented parent path, CLI command, env var, REST route, and permission key still resolves upstream.

## ⚠ Reconcile

- **Navigation-path inconsistency (doc-internal, not upstream drift).** The new custom-sandbox-images page follows the parent UI breadcrumb **Settings → Instance settings → Environments** (from `CompanyEnvironments.tsx`), but the existing `docs/reference/adapters/sandbox-providers.md` says **"Company Settings → Environments."** Reconcile the two to one wording repo-wide (the code supports "Settings → Instance settings → Environments").
- No previously-applied entries disappeared from the cumulative manifest.
