# PENDING — nightly docs sync

Regenerated by `/sync-docs` in **nightly mode**. This is the current cumulative view from the last parent release, rebased onto the new release baseline.

**Baseline reset.** Release **v2026.722.0** shipped to `main` (docs PR #72) and `nightly` was realigned onto it. The sync baseline is now `base_release_tag = v2026.722.0` (`e55d702`). There are no pending nightly drafts against this baseline yet — the next nightly run will regenerate this file with the cumulative window `v2026.722.0 → parent master HEAD`.

_Nothing pending._
